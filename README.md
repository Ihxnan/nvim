## 环境准备
确保您的系统上安装了以下工具和软件：
- Node.js
- npm
- curl
- git
- unzip
- Python
- mingw
- PowerShell

## 安装步骤
1. 下载项目zip压缩包。
2. 将压缩包重命名为项目名称。
3. 解压到 `C:\Users\你的用户名\AppData\Local\nvim`。
4. 打开终端并导航到 `nvim` 目录。
5. 运行配置脚本（如果有），等待安装完成。

## 检查环境
安装完成后，请在终端打开 `nvim` 并执行以下命令检查环境配置：
```sh
:checkhealth
```
